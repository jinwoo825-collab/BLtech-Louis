# 개인 데이터 / 런타임 생성 파일
pms/pms-data.json
*.tmp

# 로컬 전용 설정 (개인 PC에만)
.claude/settings.local.json

# 실제 API 키 등 비밀값 (.env.example 만 커밋, 실제 .env 는 절대 커밋 금지)
.env
.env.local
.env*.local

# 의존성
node_modules/

# OneDrive / Windows 잡파일
desktop.ini
Thumbs.db
ehthumbs.db
~$*
*.bak
